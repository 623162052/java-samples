深入：JVM
--------------------------------------------------------------
	JVM将结束：
		1.System.exit(int flag);		//非0表示异常结束
		2.程序正常执行结束
		3.程序执行异常会错误而终止
		4.操作系统错误导致JVM进程终止
------------------------------------------------------------
	类的加载：将类的.class文件的二进制数据读入内存
		自带加载器：Bootstrap、Extension、System
		自定义加载器：ClassLoader（抽象类）
	连接：
	初始化：

------------------------------------------------------------
	ClassLoader